coverage:
  precision: 2
  round: down
  range: "70...100"
  # always pass the coverage checks
  status:
    project:
      default:
        target: 0%
        threshold: 100%
    patch:
      default:
        target: 0%
        threshold: 100%
